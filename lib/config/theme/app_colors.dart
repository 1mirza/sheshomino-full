import 'package:flutter/material.dart';

// این کلاس تمام رنگ‌های مورد استفاده در برنامه را نگهداری می‌کند
class AppColors {
  // رنگ‌های اصلی
  static const Color primary = Color(0xFF00897B); // Teal Darker
  static const Color primaryLight = Color(0xFF4DB6AC); // Teal Lighter
  static const Color accent = Color(0xFFFFC107); // Amber

  // رنگ‌های پس‌زمینه
  static const Color background = Color(0xFFF5F5F5); // Light Grey
  static const Color cardBackground = Colors.white;

  // رنگ‌های متن
  static const Color textPrimary = Color(0xFF212121); // Dark Grey
  static const Color textSecondary = Color(0xFF757575); // Medium Grey

  // رنگ‌های دیگر
  static const Color success = Colors.green;
  static const Color error = Colors.red;
}
